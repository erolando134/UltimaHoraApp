<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Panel de Administración - Última Hora</title>

  <!-- Leaflet CSS -->
  <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css"/>

  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f4f4f4;
      margin: 0;
      padding: 0;
    }
    header {
      background-color: #212121;
      color: white;
      padding: 1rem;
      text-align: center;
    }
    main {
      padding: 2rem;
      max-width: 1000px;
      margin: auto;
    }
    .card {
      background: white;
      border-radius: 10px;
      box-shadow: 0 2px 6px rgba(0,0,0,0.15);
      padding: 1.5rem;
      margin-bottom: 2rem;
    }
    h2 {
      margin-top: 0;
    }
    .botones {
      display: flex;
      flex-wrap: wrap;
      gap: 1rem;
      margin-top: 1rem;
    }
    .botones button {
      flex: 1 1 200px;
      padding: 1rem;
      background-color: #0288d1;
      color: white;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      transition: background 0.3s;
    }
    .botones button:hover {
      background-color: #0277bd;
    }
    #map {
      height: 400px;
      margin-top: 1rem;
      border-radius: 10px;
    }
    .alerta {
      padding: 1rem;
      background-color: #ffe082;
      color: #5d4037;
      border-left: 5px solid #ffca28;
      margin-bottom: 1rem;
      border-radius: 5px;
    }
  </style>
</head>
<body>

  <header>
    <h1>Administración - Última Hora</h1>
  </header>

  <main>
    <div class="card alerta">
      <strong>Atención:</strong> Revisa diariamente los reportes y depósitos de los chóferes para evitar interrupciones en el servicio.
    </div>

    <div class="card">
      <h2>Resumen del Sistema</h2>
      <p>Total de chóferes activos: <strong>128</strong></p>
      <p>Solicitudes de viaje hoy: <strong>245</strong></p>
      <p>Reportes por revisar: <strong>3</strong></p>
    </div>

    <div class="card">
      <h2>Mapa de cobertura nacional (Cuba)</h2>
      <div id="map"></div>
    </div>

    <div class="card">
      <h2>Gestión rápida</h2>
      <div class="botones">
        <button onclick="alert('Listado de chóferes cargado')">Ver chóferes</button>
        <button onclick="alert('Revisar depósitos')">Revisar depósitos</button>
        <button onclick="alert('Configuraciones abiertas')">Configuración</button>
        <button onclick="alert('Cerrar sesión')">Cerrar sesión</button>
      </div>
    </div>
  </main>

  <!-- Leaflet JS -->
  <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>
  <script>
    // Crear mapa centrado en Cuba
    document.addEventListener("DOMContentLoaded", function () {
      const map = L.map('map').setView([21.5, -79.5], 6); // Coordenadas centro de Cuba

      L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
        attribution: '© OpenStreetMap contributors'
      }).addTo(map);

      // Puedes añadir marcadores si lo deseas por provincias
      // Ejemplo:
      // L.marker([23.13, -82.38]).addTo(map).bindPopup("La Habana").openPopup();
    });
  </script>

</body>
</html>
